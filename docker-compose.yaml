version: '3.8'

services:
  ThreeFixes_Example:
    image: tritlo/endemic:unstable
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./examples/:/input
      - ./resources:/config
      - ./compose-output:/output/:rw
    environment:
      REPAIR_TARGET: /input/FourFixes.hs
      CONFIG_FILE: /config/sample_config.json
      LOG_LEVEL: INFO
    oom_kill_disable: true